name: analytic_base
resolution: workspace
description: "Provider-agnostic analytics base layer for Flutter. Defines the domain contract, value objects, and BLoC for event tracking, screen logging, and user identification."
version: 0.0.1
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc:
  codebase: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
